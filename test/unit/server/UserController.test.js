var request = require('supertest');
var sinon = require('sinon');

describe('UserController', function() {
  describe('#findOne()', function() {
    
    // it('should respond with nothing when an unknown user is requested', function (done) {
    //   request(sails.hooks.http.app)
    //     .get('/user/findOne/1')
    //     .expect(true, true);
    // });

    // it('should respond with object containing the user requested', function (done) {
    //     
    //   request(sails.hooks.http.app)
    //     .get('/user/findOne/2')
    //     .expect(true, true);
    // });
  });

});
